import {state,esc,formatDate} from './state.js';
import {ensureMedia,maybeCreatePressEvent,mediaPressureLabel} from './media-engine.js';
export function mediaPage(){const m=ensureMedia(state.save);maybeCreatePressEvent(state.save);const pending=m.pending[0];return `<div class="hero"><div><span class="eyebrow">IMPRENSA & REPUTAÇÃO</span><h1>Mídia</h1><p>Pressão ${mediaPressureLabel(m.pressure)} • ${m.pressure}/100</p></div><span class="badge">${m.history.length} coletiva(s) registradas</span></div>${pending?`<div class="card press-event"><span class="eyebrow">COLETIVA PENDENTE • ${formatDate(pending.date)}</span><h2>${esc(pending.title)}</h2><p class="press-question">${esc(pending.question)}</p><div class="press-options">${pending.options.map(o=>`<button class="press-option" data-press-answer="${o.id}" data-press-event="${pending.id}"><b>${esc(o.label)}</b><small>Torcida ${signed(o.effects.fans)} • Conselho ${signed(o.effects.board)} • Técnico ${signed(o.effects.coach)}</small></button>`).join('')}</div></div>`:`<div class="card"><h2>Nenhuma coletiva obrigatória</h2><p class="subtle">Grandes resultados, clássicos, crises e transferências podem gerar novas perguntas.</p></div>`}<h2 class="section-title">Histórico de declarações</h2><div class="feed">${m.history.slice(0,30).map(h=>`<div class="news"><span>${formatDate(h.date)}</span><b>${esc(h.title)}</b><p>${esc(h.answer||'Sem resposta registrada')}</p></div>`).join('')||'<div class="card subtle">Nenhuma entrevista respondida ainda.</div>'}</div>`}
const signed=v=>v>0?`+${v}`:`${v}`;
export function mediaMini(){const m=ensureMedia(state.save);maybeCreatePressEvent(state.save);return `<div class="card media-mini"><div class="standings-title"><div><span class="eyebrow">IMPRENSA</span><h2>${m.pending.length?'Coletiva aguardando resposta':`Pressão ${mediaPressureLabel(m.pressure)}`}</h2></div><button class="button ghost compact" data-page="media">Abrir mídia</button></div></div>`}
