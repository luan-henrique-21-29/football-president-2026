const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const esc=v=>String(v??'').replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]));
function weightedPick(list){if(!list?.length)return null;const weights=list.map(p=>Math.max(1,(p.overall||70)-55)),total=weights.reduce((a,b)=>a+b,0);let r=Math.random()*total;for(let i=0;i<list.length;i++){r-=weights[i];if(r<=0)return list[i]}return list.at(-1)}
function roleOf(p){const x=`${p?.position||''} ${p?.subPosition||''}`.toLowerCase();if(/goal|keeper/.test(x))return'GK';if(/defen|back/.test(x))return'DEF';if(/mid|wing/.test(x))return'MID';return'ATT'}
function rowAnchors(count,x){if(count<=1)return[[x,50]];return Array.from({length:count},(_,i)=>[x,12+(76*i/(count-1))])}
function formationAnchors(shape={GK:1,DEF:4,MID:3,ATT:3}){return[...rowAnchors(shape.GK||1,7).map(a=>['GK',...a]),...rowAnchors(shape.DEF||4,23).map(a=>['DEF',...a]),...rowAnchors(shape.MID||3,47).map(a=>['MID',...a]),...rowAnchors(shape.ATT||3,70).map(a=>['ATT',...a])].slice(0,11)}
function makePlayers(team,starters=[],shape,profile={}){const anchors=formationAnchors(shape),byRole={GK:[],DEF:[],MID:[],ATT:[]};starters.forEach(p=>byRole[roleOf(p)].push(p));const used=new Set(),ordered=[];for(const [role] of anchors){let p=byRole[role].find(x=>!used.has(String(x.id)));if(!p)p=starters.find(x=>!used.has(String(x.id)));if(p)used.add(String(p.id));ordered.push(p||null)}return anchors.map(([role,x,y],i)=>{const p=ordered[i],baseX=team?100-x:x;return{team,i,role,ax:baseX,ay:y,x:baseX,y,tx:baseX,ty:y,speed:.46+Math.random()*.12,label:p?.name?.split(/\s+/).at(-1)||String(i+1),overall:p?.overall||70,profile}})}
function minutePool(count){const used=new Set(),out=[];while(out.length<count){const m=Math.floor(3+Math.random()*87);if(!used.has(m)){used.add(m);out.push(m)}}return out.sort((a,b)=>a-b)}
function buildEvents({result,homeName,awayName,userTeam,starters}){const events=[],homeGoals=userTeam===0?result.gf:result.ga,awayGoals=userTeam===0?result.ga:result.gf,totalGoals=homeGoals+awayGoals,totalExtras=Math.min(26,Math.max(12,Math.round((result.shots+result.shotsAgainst)*.72))),minutes=minutePool(totalGoals+totalExtras+6),goalMinutes=minutes.splice(0,totalGoals),userAttackers=(starters||[]).filter(p=>/attack|forward|winger|striker|midfield/i.test(`${p.position||''} ${p.subPosition||''}`)),userCandidates=userAttackers.length?userAttackers:starters||[];let gi=0;const addGoals=(team,count)=>{for(let i=0;i<count;i++){const m=goalMinutes[gi++]||Math.floor(10+Math.random()*75),scorer=team===userTeam?weightedPick(userCandidates):null;events.push({m,type:'goal',team,text:scorer?`GOOOL! ${scorer.name} marca para o ${team===0?homeName:awayName}.`:`GOOOL do ${team===0?homeName:awayName}!`,player:scorer?.name||''})}};addGoals(0,homeGoals);addGoals(1,awayGoals);for(let i=0;i<totalExtras;i++){const m=minutes[i]||Math.floor(4+Math.random()*84),team=Math.random()<(result.possession/100)?userTeam:1-userTeam,r=Math.random(),teamName=team===0?homeName:awayName;if(r<.10)events.push({m,type:'card',team,text:`Cartão amarelo para o ${teamName}.`});else if(r<.23)events.push({m,type:'corner',team,text:`Escanteio para o ${teamName}.`});else if(r<.38)events.push({m,type:'save',team,text:`Finalização do ${teamName} e grande defesa do goleiro!`});else if(r<.51)events.push({m,type:'shot',team,text:`${teamName} finaliza com perigo.`});else if(r<.63)events.push({m,type:'foul',team,text:`Falta marcada a favor do ${teamName}.`});else if(r<.72)events.push({m,type:'offside',team,text:`Impedimento do ${teamName}.`});else events.push({m,type:'chance',team,text:`Boa construção do ${teamName} no campo de ataque.`})}[58,66,74,81].forEach((m,i)=>events.push({m,type:'sub',team:i%2,text:`Substituição no ${i%2===0?homeName:awayName}. O treinador mexe pensando também no calendário.`}));events.push({m:45,type:'half',team:null,text:'Intervalo.'});return events.sort((a,b)=>a.m-b.m||((a.type==='goal')?-1:1))}
const balanced={pressing:55,defensiveLine:52,width:58,tempo:58,possessionBias:52,directness:52};
export function showMatchViewer({app,club,fixture,result,coachName,onFinish}){const homeName=fixture.home?club.name:fixture.opponentName,awayName=fixture.home?fixture.opponentName:club.name,userTeam=fixture.home?0:1,displayResult={home:userTeam===0?result.gf:result.ga,away:userTeam===0?result.ga:result.gf},userStarters=result.plan?.starters||[],profile=result.plan?.coachProfile||balanced,shape=profile.shape||{GK:1,DEF:4,MID:3,ATT:3},events=buildEvents({result,homeName,awayName,userTeam,starters:userStarters});app.innerHTML=`<section class="live-match"><header class="live-head"><button class="button ghost compact" id="leaveMatch">PULAR AO FIM</button><div><span>${esc(fixture.competition)} • ${esc(fixture.round||'PARTIDA')}</span><strong><b id="homeScore">0</b> <em>–</em> <b id="awayScore">0</b></strong><small><i id="matchMinute">0</i>'</small></div><div class="speed-controls"><button id="pauseMatch" title="Pausar">Ⅱ</button><button data-speed="1" class="active">1x</button><button data-speed="2">2x</button><button data-speed="4">4x</button><button data-speed="8">8x</button></div></header><div class="live-body"><div class="pitch-shell"><canvas id="matchCanvas"></canvas><div class="score-names"><span>${esc(homeName)}</span><span>${esc(awayName)}</span></div><div id="goalOverlay" class="goal-overlay"><span>GOOOL!</span></div><div id="matchFinished" class="cd-match-finished" hidden></div></div><aside class="live-panel"><div class="live-coach"><span>PLANO DE ${esc(coachName)}</span><b>${esc(result.plan?.preferredFormation||'Formação variável')} • ${esc(result.plan?.rotation>=.4?'Time misto':result.plan?.rotation>=.15?'Rodízio leve':'Força máxima')}</b><small>${esc(result.plan?.reason||'O treinador definiu a equipe para este compromisso.')}</small><div class="cd-live-tactic-tags"><i>${esc(profile.pressLabel||'Pressão média')}</i><i>${esc(profile.lineLabel||'Linha média')}</i><i>${esc(profile.buildUp||'Construção mista')}</i></div></div><div class="live-stats"><div><span>Posse</span><b id="statPoss">50–50</b></div><div><span>Finalizações</span><b id="statShots">0–0</b></div><div><span>xG</span><b id="statXg">0.00–0.00</b></div><div><span>Cartões</span><b id="statCards">0–0</b></div><div><span>Substituições</span><b id="statSubs">0–0</b></div><div><span>OVR escalação</span><b>${result.plan?.lineupOverall||'—'}</b></div></div><h3>Minuto a minuto</h3><div id="commentary" class="commentary"><div><strong>0'</strong> — Bola rolando. A equipe ocupa o campo conforme a ideia do treinador.</div></div></aside></div></section>`;
 const canvas=document.querySelector('#matchCanvas'),ctx=canvas.getContext('2d'),homeStarters=userTeam===0?userStarters:[],awayStarters=userTeam===1?userStarters:[],homeProfile=userTeam===0?profile:balanced,awayProfile=userTeam===1?profile:balanced,homeShape=userTeam===0?shape:{GK:1,DEF:4,MID:4,ATT:2},awayShape=userTeam===1?shape:{GK:1,DEF:4,MID:4,ATT:2},players=[...makePlayers(0,homeStarters,homeShape,homeProfile),...makePlayers(1,awayStarters,awayShape,awayProfile)],ball={x:50,y:50,tx:50,ty:50,ownerTeam:null};let minuteNow=0,speed=1,paused=false,last=performance.now(),acc=0,eventIndex=0,ended=false,score=[0,0],cards=[0,0],subs=[0,0],possessionTeam=Math.random()<.5?0:1;const msPerMinute=120000/90;
 function resize(){const dpr=Math.min(2,devicePixelRatio||1),w=Math.max(1,canvas.clientWidth),h=Math.max(1,canvas.clientHeight);canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}resize();window.addEventListener('resize',resize);document.querySelectorAll('[data-speed]').forEach(b=>b.onclick=()=>{speed=Number(b.dataset.speed);paused=false;document.querySelector('#pauseMatch')?.classList.remove('active');document.querySelectorAll('[data-speed]').forEach(x=>x.classList.toggle('active',x===b))});document.querySelector('#pauseMatch').onclick=e=>{paused=!paused;e.currentTarget.classList.toggle('active',paused);e.currentTarget.textContent=paused?'▶':'Ⅱ'};document.querySelector('#leaveMatch').onclick=()=>finish(true);
 function commentary(ev){const d=document.createElement('div');d.className=ev.type;d.innerHTML=`<strong>${ev.m}'</strong> — ${esc(ev.text)}`;const root=document.querySelector('#commentary');root.prepend(d);while(root.children.length>14)root.lastElementChild.remove()}
 function processEvents(){while(eventIndex<events.length&&events[eventIndex].m<=minuteNow){const e=events[eventIndex++];if(e.type==='goal'){score[e.team]++;document.querySelector('#homeScore').textContent=score[0];document.querySelector('#awayScore').textContent=score[1];flashGoal(e.team)}if(e.type==='card')cards[e.team]++;if(e.type==='sub')subs[e.team]++;commentary(e);if(e.team!=null){possessionTeam=e.team;ball.ownerTeam=e.team;ball.tx=e.team===0?84:16;ball.ty=15+Math.random()*70}}}
 function flashGoal(team){const root=document.querySelector('.live-match'),ov=document.querySelector('#goalOverlay');root.classList.add('goal');ov.classList.add('show');ov.querySelector('span').textContent=`GOOOL ${team===0?homeName:awayName}!`;setTimeout(()=>{root?.classList.remove('goal');ov?.classList.remove('show')},900)}
 function updateStats(){const ratio=Math.min(1,minuteNow/90),targetPoss=userTeam===0?result.possession:100-result.possession,p=Math.round(50+(targetPoss-50)*ratio),hs=Math.round((userTeam===0?result.shots:result.shotsAgainst)*ratio),as=Math.round((userTeam===0?result.shotsAgainst:result.shots)*ratio),hx=(userTeam===0?result.xg:result.xga)*ratio,ax=(userTeam===0?result.xga:result.xg)*ratio;document.querySelector('#statPoss').textContent=`${p}–${100-p}`;document.querySelector('#statShots').textContent=`${hs}–${as}`;document.querySelector('#statXg').textContent=`${hx.toFixed(2)}–${ax.toFixed(2)}`;document.querySelector('#statCards').textContent=`${cards[0]}–${cards[1]}`;document.querySelector('#statSubs').textContent=`${subs[0]}–${subs[1]}`}
 function tacticalTarget(p){const prof=p.profile||balanced,attackDir=p.team===0?1:-1,hasBall=possessionTeam===p.team,ballX=ball.x,ballY=ball.y,lineShift=(Number(prof.defensiveLine||52)-50)*.12,press=(Number(prof.pressing||55)-50)/50,width=(Number(prof.width||58)-50)/50,tempo=(Number(prof.tempo||58)-50)/50;let x=p.ax,y=p.ay;if(p.role==='DEF')x+=attackDir*lineShift;if(hasBall){const push=p.role==='ATT'?11:p.role==='MID'?8:p.role==='DEF'?3:0;x+=attackDir*(push+tempo*4);y=50+(p.ay-50)*(1+width*.16)}else{const compact=p.role==='ATT'?4:p.role==='MID'?6:p.role==='DEF'?3:0;x-=attackDir*compact;y=50+(p.ay-50)*(1-width*.12);const dist=Math.hypot(ballX-p.x,ballY-p.y);if(dist<28&&p.role!=='GK'){x+=(ballX-x)*(.07+Math.max(0,press)*.13);y+=(ballY-y)*(.07+Math.max(0,press)*.13)}}if(p.role==='GK')x=p.ax;return{x:clamp(x,4,96),y:clamp(y,5,95)}}
 function move(dt){if(Math.random()<.0045){possessionTeam=1-possessionTeam;ball.ownerTeam=possessionTeam}for(const p of players){const t=tacticalTarget(p);p.tx=t.x;p.ty=t.y;if(possessionTeam===p.team&&p.role!=='GK'&&Math.hypot(ball.x-p.x,ball.y-p.y)<16){p.tx+=p.team===0?4:-4;p.ty+=(Math.random()-.5)*4}const dx=p.tx-p.x,dy=p.ty-p.y,l=Math.hypot(dx,dy)||1;p.x+=dx/l*p.speed*dt*.045;p.y+=dy/l*p.speed*dt*.045}if(Math.hypot(ball.tx-ball.x,ball.ty-ball.y)<2||Math.random()<.012){const candidates=players.filter(p=>p.team===possessionTeam&&p.role!=='GK'),carrier=candidates[Math.floor(Math.random()*candidates.length)]||players[0];ball.tx=carrier.x+(carrier.team===0?5:-5);ball.ty=carrier.y+(Math.random()-.5)*5}const dx=ball.tx-ball.x,dy=ball.ty-ball.y,l=Math.hypot(dx,dy)||1;ball.x+=dx/l*dt*.105;ball.y+=dy/l*dt*.105}
 function drawPitch(w,h){ctx.fillStyle='#61bce8';ctx.fillRect(0,0,w,h);for(let i=0;i<10;i++){ctx.fillStyle=i%2?'rgba(255,255,255,.04)':'rgba(19,88,126,.025)';ctx.fillRect(i*w/10,0,w/10,h)}ctx.strokeStyle='rgba(255,255,255,.84)';ctx.lineWidth=2;ctx.strokeRect(16,16,w-32,h-32);ctx.beginPath();ctx.moveTo(w/2,16);ctx.lineTo(w/2,h-16);ctx.stroke();ctx.beginPath();ctx.arc(w/2,h/2,Math.min(w,h)*.13,0,Math.PI*2);ctx.stroke();ctx.strokeRect(16,h*.27,w*.13,h*.46);ctx.strokeRect(w-16-w*.13,h*.27,w*.13,h*.46)}
 function draw(){const w=canvas.clientWidth,h=canvas.clientHeight;ctx.clearRect(0,0,w,h);drawPitch(w,h);for(const p of players){const x=p.x/100*w,y=p.y/100*h;ctx.beginPath();ctx.fillStyle=p.team===0?'#fff':'#ff6670';ctx.arc(x,y,Math.max(4,Math.min(7,w/160)),0,Math.PI*2);ctx.fill();ctx.strokeStyle=p.team===0?'#2484b8':'#a72e39';ctx.lineWidth=1.5;ctx.stroke();if(w>600&&p.label){ctx.font='10px Inter, sans-serif';ctx.textAlign='center';ctx.fillStyle='#17445f';ctx.fillText(String(p.label).slice(0,11),x,y-10)}}ctx.beginPath();ctx.fillStyle='#f9c74f';ctx.arc(ball.x/100*w,ball.y/100*h,3.4,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#8f6a08';ctx.stroke()}
 function loop(t){if(ended)return;const dt=Math.min(45,t-last);last=t;if(!paused){acc+=dt*speed;while(acc>=msPerMinute){acc-=msPerMinute;minuteNow++;document.querySelector('#matchMinute').textContent=Math.min(90,minuteNow);processEvents();updateStats();if(minuteNow>=90){finish(false);return}}move(dt*speed)}draw();requestAnimationFrame(loop)}
 function finish(skipped){if(ended)return;ended=true;paused=true;window.removeEventListener('resize',resize);score=[displayResult.home,displayResult.away];document.querySelector('#homeScore').textContent=score[0];document.querySelector('#awayScore').textContent=score[1];document.querySelector('#matchMinute').textContent=90;minuteNow=90;updateStats();if(!skipped)commentary({m:90,type:'half',text:'Fim de jogo.'});const panel=document.querySelector('#matchFinished');panel.hidden=false;panel.innerHTML=`<div class="cd-match-finished-card"><span>FIM DE JOGO</span><h2>${esc(homeName)} <b>${score[0]}–${score[1]}</b> ${esc(awayName)}</h2><div><i>Posse <b>${userTeam===0?result.possession:100-result.possession}%–${userTeam===0?100-result.possession:result.possession}%</b></i><i>Finalizações <b>${userTeam===0?result.shots:result.shotsAgainst}–${userTeam===0?result.shotsAgainst:result.shots}</b></i><i>xG <b>${(userTeam===0?result.xg:result.xga).toFixed(2)}–${(userTeam===0?result.xga:result.xg).toFixed(2)}</b></i></div><p>${esc(result.plan?.preferredFormation||'')} • ${esc(result.plan?.style||'')} • OVR ${result.plan?.lineupOverall||'—'}</p><button class="button big" id="finalizeWatchedMatch">FINALIZAR PARTIDA</button></div>`;document.querySelector('#finalizeWatchedMatch').onclick=()=>onFinish({skipped})}
 requestAnimationFrame(loop)}
