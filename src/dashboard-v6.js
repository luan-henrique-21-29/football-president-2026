import {dashboardPage as baseDashboard} from './dashboard-v5.js';
import {state,esc} from './state.js';
import {coachGameplayProfile} from './coach-profile.js';
import {ensurePlan2} from './plan2-engine.js';
export function dashboardPage(){ensurePlan2(state.save);const p=coachGameplayProfile(state.save.coach||{}),offers=state.save.transferInbox.filter(o=>o.status==='PENDING').length,reports=state.save.scouting.reports.length,missions=state.save.scouting.assignments.filter(a=>a.status==='ACTIVE').length;return `${baseDashboard()}<h2 class="section-title">Futebol & mercado</h2><div class="grid grid-3 cd-dashboard-plan2"><button class="card cd-dash-link" data-page="coach"><span class="eyebrow">TÁTICA DO TÉCNICO</span><h2>${esc(p.preferredFormation)}</h2><p>${esc(p.style)} • ${esc(p.pressLabel)} • ${esc(p.lineLabel)}</p><b>Ver centro tático ›</b></button><button class="card cd-dash-link" data-page="scouting"><span class="eyebrow">SCOUTING</span><h2>${missions} missão(ões)</h2><p>${reports} relatório(s) disponíveis</p><b>Abrir olheiros ›</b></button><button class="card cd-dash-link" data-page="inbox"><span class="eyebrow">PROPOSTAS RECEBIDAS</span><h2>${offers}</h2><p>Compras e empréstimos de outros clubes</p><b>Abrir central ›</b></button></div>`}
