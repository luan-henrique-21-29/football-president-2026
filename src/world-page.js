import {state,esc,formatDate} from './state.js';
import {marketCenterPage} from './market-center-page.js';
export function worldPage(){const news=state.save.worldNews||[],coaches=Object.entries(state.save.worldCoachOverrides||{}).slice(-12).reverse();return `${marketCenterPage()}<div class="grid grid-2"><div><h2 class="section-title">Notícias do mundo</h2><div class="feed">${news.slice(0,20).map(n=>`<div class="news"><span>${formatDate(n.date)}</span><b>${esc(n.title)}</b><p>${esc(n.body)}</p></div>`).join('')||'<div class="card subtle">As notícias aparecem conforme a carreira avança.</div>'}</div></div><div><h2 class="section-title">Trocas de técnicos</h2><div class="feed">${coaches.map(([clubId,name])=>`<div class="news"><b>${esc(name)}</b><p>Mudança registrada no mundo da carreira • clube ID ${esc(clubId)}</p></div>`).join('')||'<div class="card subtle">Nenhuma troca de técnico registrada.</div>'}</div></div></div>`}
