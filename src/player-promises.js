import {ensurePlayerDynamics,recentStartRate} from './player-dynamics.js';
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
export function processPlayerPromises(save,date){ensurePlayerDynamics(save);const resolved=[];for(const promise of save.playerPromises.filter(x=>x.status==='ACTIVE'&&x.until<=date)){const rate=recentStartRate(save,promise.playerId,8),dyn=save.playerDynamics[String(promise.playerId)]??={happiness:72};if(rate>=(promise.targetStartRate||.55)){promise.status='FULFILLED';dyn.happiness=clamp((dyn.happiness||72)+6,0,100);save.news?.unshift({date,title:'Promessa de minutos cumprida',body:'O atleta recebeu a utilização esportiva combinada com a presidência.'})}else{promise.status='BROKEN';dyn.happiness=clamp((dyn.happiness||72)-18,0,100);save.news?.unshift({date,title:'Promessa de minutos não cumprida',body:'A utilização esportiva ficou abaixo do que havia sido combinado com a presidência.'})}resolved.push({...promise,startRate:rate})}return resolved}
