import {state,esc,formatDate,contractLabel,normalizedName} from './state.js';
function pool(){const employed=state.world.clubs.filter(x=>x.coachName).map(x=>({id:String(x.id),name:x.coachName,club:x.name,country:x.country,reputation:x.teamOverall||72,status:'EMPLOYED',formation:'Variável',style:(x.teamOverall||72)>=84?'Ambicioso':'Equilibrado'}));const free=(state.save.coachHistory||[]).filter(h=>h.name).map((h,i)=>({id:`free-${i}`,name:h.name,club:'Sem clube',country:'',reputation:72,status:'UNEMPLOYED',formation:'Variável',style:'Adaptável'}));return[...new Map([...employed,...free].map(c=>[c.name,c])).values()].filter(c=>c.name!==state.save.coach?.name)}
export function coachPage(){const c=state.save.coach,all=pool();return `<div class="hero"><div><span class="eyebrow">COMANDO TÉCNICO</span><h1>Treinadores</h1><p>Empregados, livres e técnicos que ficaram disponíveis durante a carreira.</p></div><span class="badge">Relação ${state.save.relation}/100</span></div><div class="coach-profile card"><div class="coach-avatar">${esc((c?.name||'?').split(/\s+/).map(x=>x[0]).slice(0,2).join(''))}</div><div><span class="eyebrow">TÉCNICO ATUAL • ${esc(c?.status||'EMPLOYED')}</span><h2>${esc(c?.name||'Não informado')}</h2><p>${c?.original?'Veio com o clube na database inicial.':`Contratado em ${formatDate(c?.hiredDate)}.`}</p></div><div class="coach-contract"><span>CONTRATO RESTANTE</span><b>${c?.contractUntil?contractLabel(c.contractUntil):'Não informado na fonte'}</b><small>${c?.contractUntil?`até ${formatDate(c.contractUntil)}`:'O jogo não inventa duração não confirmada.'}</small></div><button class="button danger" id="fireCoach">Demitir treinador</button></div><div class="market-toolbar card coach-filters"><input id="coachSearch" placeholder="Nome ou clube"><select id="coachStatus"><option value="">Todos</option><option value="EMPLOYED">Empregado</option><option value="UNEMPLOYED">Desempregado</option></select><select id="coachRep"><option value="0">Qualquer reputação</option><option value="85">85+</option><option value="80">80+</option><option value="75">75+</option></select><button class="button secondary" id="coachFilter">Filtrar</button></div><div id="coachResults">${coachResults(all,'','',0)}</div>`;}
export function coachResults(all,search,status,minRep){const q=normalizedName(search),filtered=all.filter(c=>(!q||normalizedName(`${c.name} ${c.club}`).includes(q))&&(!status||c.status===status)&&c.reputation>=minRep).sort((a,b)=>b.reputation-a.reputation);return `<div class="coach-market">${filtered.slice(0,120).map(c=>`<div class="coach-card card"><span>${esc(c.status)} • REP ${c.reputation}</span><h3>${esc(c.name)}</h3><p>${esc(c.club)}${c.country?` • ${esc(c.country)}`:''}</p><small>${esc(c.style)} • ${esc(c.formation)}</small><button class="button secondary full" data-hire-coach="${esc(c.id)}" data-free-coach="${c.status==='UNEMPLOYED'?'true':'false'}" data-coach-name="${esc(c.name)}">Abordar treinador</button></div>`).join('')||'<div class="card subtle">Nenhum treinador encontrado.</div>'}</div>`;}
