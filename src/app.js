const app=document.querySelector('#app');const S='football-president-2026-save-v1';let db=null,save=null,page='dashboard';const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(v||0);const club=()=>db.clubs.find(c=>c.id===save.clubId);async function init(){db=await fetch('./data/clubs.json').then(r=>{if(!r.ok)throw Error('Falha ao carregar datapack');return r.json()});try{save=JSON.parse(localStorage.getItem(S))}catch{};if(save&&!db.clubs.some(c=>c.id===save.clubId))save=null;render()}function persist(){localStorage.setItem(S,JSON.stringify(save))}function newSave(){const id=document.querySelector('#clubSelect').value,c=db.clubs.find(x=>x.id===id);save={version:1,databaseSnapshot:db.snapshot,president:{name:document.querySelector('#presidentName').value||'Presidente',nationality:document.querySelector('#nationality').value||'Brasil',style:document.querySelector('#style').value,reputation:50},clubId:id,season:'2026/27',week:1,cash:Math.round(c.budget*1.35),transferBudget:c.budget,wageBudget:Math.round(c.budget*.36),fanTrust:68,boardTrust:72,wins:0,draws:0,losses:0,goalsFor:0,goalsAgainst:0,coach:null,transactions:[],news:[{title:'Nova presidência começa hoje',body:`${document.querySelector('#presidentName').value||'O novo presidente'} assume o comando do ${c.name}.`},{title:'Planejamento da temporada',body:'A diretoria aguarda as prioridades esportivas e financeiras.'}]};persist();render()}const nav=[['dashboard','Painel'],['club','Clube'],['coach','Treinador'],['market','Mercado'],['finances','Finanças'],['stadium','Estádio'],['news','Notícias'],['database','Base de dados'],['settings','Configurações']];function frame(body){const c=club();app.innerHTML=`<header class="topbar"><div class="brand"><div class="brand-mark">FP</div><div class="brand-copy"><strong>FOOTBALL PRESIDENT</strong><span>${c.name} • Base 14/08/2026</span></div></div><button class="button ghost" data-action="fullscreen">Tela cheia</button></header><div class="layout"><aside class="sidebar">${nav.map(([id,label])=>`<button class="nav-btn ${page===id?'active':''}" data-page="${id}">${label}</button>`).join('')}</aside><main class="content">${body}</main></div>`;document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{page=b.dataset.page;render()});document.querySelector('[data-action="fullscreen"]')?.addEventListener('click',()=>document.documentElement.requestFullscreen?.())}function dashboard(){const s=save,c=club();frame(`<div class="hero"><div><h1>${c.name}</h1><p>${s.president.name}, presidente • Semana ${s.week}</p></div><button class="button" id="advance">Avançar semana</button></div><div class="grid grid-4"><div class="card"><h3>Caixa</h3><div class="metric">${money(s.cash)}</div></div><div class="card"><h3>Transferências</h3><div class="metric">${money(s.transferBudget)}</div></div><div class="card"><h3>Torcida</h3><div class="metric">${Math.round(s.fanTrust)}%</div></div><div class="card"><h3>Conselho</h3><div class="metric">${Math.round(s.boardTrust)}%</div></div></div><h2 class="section-title">Situação esportiva</h2><div class="grid grid-3"><div class="card"><h3>Treinador</h3><div class="metric" style="font-size:20px">${s.coach?.name||'Informação não disponível na base atual.'}</div><p class="subtle">O treinador decide formação, escalação e substituições.</p></div><div class="card"><h3>Campanha</h3><div class="metric">${s.wins}V ${s.draws}E ${s.losses}D</div><p class="subtle">Gols ${s.goalsFor}–${s.goalsAgainst}</p></div><div class="card"><h3>Estádio</h3><div class="metric" style="font-size:20px">${c.stadium}</div></div></div><h2 class="section-title">Notícias</h2><div class="feed">${s.news.slice(0,5).map(n=>`<div class="news"><b>${n.title}</b><span class="subtle">${n.body}</span></div>`).join('')}</div>`);document.querySelector('#advance').onclick=advance}function advance(){const s=save,c=club(),edge=(c.reputation-70)/30;let gf=Math.max(0,Math.round(Math.random()*3+edge*.5)),ga=Math.max(0,Math.round(Math.random()*3-edge*.25));if(gf>ga){s.wins++;s.fanTrust+=1.5;s.boardTrust+=.7}else if(gf===ga){s.draws++;s.fanTrust+=.1}else{s.losses++;s.fanTrust-=1.6;s.boardTrust-=.8}s.goalsFor+=gf;s.goalsAgainst+=ga;s.week++;const cost=Math.max(250000,Math.round(c.budget*.006));s.cash-=cost;s.news.unshift({title:`Semana ${s.week-1}: resultado simulado ${gf}–${ga}`,body:`Custo operacional da semana: ${money(cost)}.`});persist();render()}function clubPage(){const c=club();frame(`<div class="hero"><div><h1>Clube</h1><p>Visão institucional</p></div></div><div class="grid grid-2"><div class="card"><h3>Clube</h3><div class="metric">${c.name}</div><p class="subtle">${c.country} • ${c.league}</p></div><div class="card"><h3>Reputação</h3><div class="metric">${c.reputation}/100</div></div><div class="card"><h3>Estádio</h3><div class="metric" style="font-size:22px">${c.stadium}</div></div><div class="card"><h3>Presidente</h3><div class="metric" style="font-size:22px">${save.president.name}</div><p class="subtle">${save.president.style}</p></div></div>`)}function coachPage(){frame(`<div class="hero"><div><h1>Treinador</h1><p>Gestão do comando técnico</p></div></div><div class="card"><h3>Treinador atual</h3><div class="metric" style="font-size:24px">${save.coach?.name||'Informação não disponível na base atual.'}</div><p class="subtle">Não vou inventar técnicos para preencher a temporada inicial. O datapack real de treinadores será adicionado separadamente.</p></div>`)}function marketPage(){frame(`<div class="hero"><div><h1>Mercado</h1><p>Transferências e contratos</p></div></div><div class="card"><h3>Orçamento</h3><div class="metric">${money(save.transferBudget)}</div><p class="subtle">A lista de jogadores reais ainda será carregada no datapack. Nenhum PLAYER01 ou nome inventado.</p></div>`)}function finances(){frame(`<div class="hero"><div><h1>Finanças</h1><p>Controle presidencial</p></div></div><div class="grid grid-3"><div class="card"><h3>Caixa</h3><div class="metric">${money(save.cash)}</div></div><div class="card"><h3>Transferências</h3><div class="metric">${money(save.transferBudget)}</div></div><div class="card"><h3>Folha disponível</h3><div class="metric">${money(save.wageBudget)}</div></div></div><h2 class="section-title">Ajustar orçamento</h2><div class="card"><div class="field"><label>Transferências</label><input id="budget" type="number" min="0" max="${save.cash}" value="${save.transferBudget}"></div><button class="button" id="saveBudget" style="margin-top:12px">Salvar</button></div>`);document.querySelector('#saveBudget').onclick=()=>{const n=Number(document.querySelector('#budget').value);if(n>=0&&n<=save.cash){save.transferBudget=Math.round(n);persist();render()}else alert('Valor inválido')}}function stadium(){frame(`<div class="hero"><div><h1>Estádio</h1><p>Infraestrutura</p></div></div><div class="card"><h3>Casa do clube</h3><div class="metric">${club().stadium}</div><p class="subtle">Capacidade e projetos só serão exibidos quando confirmados no datapack factual.</p></div>`)}function news(){frame(`<div class="hero"><div><h1>Notícias</h1><p>Mundo do clube</p></div></div><div class="feed">${save.news.map(n=>`<div class="news"><b>${n.title}</b><span class="subtle">${n.body}</span></div>`).join('')}</div>`)}function database(){const leagues=[...new Set(db.clubs.map(c=>c.league))];frame(`<div class="hero"><div><h1>Base de dados</h1><p>Snapshot factual inicial</p></div><span class="badge">14/08/2026</span></div><div class="grid grid-3"><div class="card"><h3>Clubes carregados</h3><div class="metric">${db.clubs.length}</div></div><div class="card"><h3>Ligas carregadas</h3><div class="metric">${leagues.length}</div></div><div class="card"><h3>Status</h3><div class="metric" style="font-size:20px">Em expansão</div></div></div><h2 class="section-title">Clubes</h2><div class="table-wrap"><table><thead><tr><th>Clube</th><th>País</th><th>Liga</th><th>Estádio</th></tr></thead><tbody>${db.clubs.map(c=>`<tr><td>${c.name}</td><td>${c.country}</td><td>${c.league}</td><td>${c.stadium}</td></tr>`).join('')}</tbody></table></div>`)}function settings(){frame(`<div class="hero"><div><h1>Configurações</h1><p>Save local</p></div></div><div class="grid grid-2"><div class="card"><h3>Salvar</h3><p class="subtle">Autosave no navegador.</p><button class="button" id="saveNow">Salvar agora</button></div><div class="card"><h3>Nova carreira</h3><p class="subtle">Apaga o save atual.</p><button class="button ghost" id="reset">Recomeçar</button></div></div>`);document.querySelector('#saveNow').onclick=()=>{persist();alert('Salvo.')};document.querySelector('#reset').onclick=()=>{if(confirm('Apagar carreira?')){localStorage.removeItem(S);save=null;render()}}}function setup(){app.innerHTML=`<div class="welcome card"><span class="badge">Base de dados: 14/08/2026</span><h1>FOOTBALL PRESIDENT 2026</h1><p>Você é o presidente. Administra clube, orçamento, treinador, mercado e infraestrutura. O treinador é independente no campo.</p><div class="form"><div class="grid grid-2"><div class="field"><label>Nome do presidente</label><input id="presidentName" placeholder="Seu nome"></div><div class="field"><label>Nacionalidade</label><input id="nationality" value="Brasil"></div></div><div class="field"><label>Estilo</label><select id="style"><option>Personalizado</option><option>Gastador</option><option>Formador</option><option>Negociador</option><option>Ambicioso</option><option>Conservador</option></select></div><div class="field"><label>Clube</label><select id="clubSelect">${db.clubs.map(c=>`<option value="${c.id}">${c.name} — ${c.league}</option>`).join('')}</select></div><button class="button" id="start">Começar carreira</button></div><p class="subtle" style="margin-top:18px">O datapack está sendo ampliado; nenhum jogador ou treinador fictício é usado para fingir que a base está completa.</p></div>`;document.querySelector('#start').onclick=newSave}function render(){if(!save)return setup();({dashboard,club:clubPage,coach:coachPage,market:marketPage,finances,stadium,news,database,settings}[page]||dashboard)()}init().catch(e=>app.innerHTML=`<div class="welcome card"><h1>Erro ao iniciar</h1><p>${e.message}</p></div>`);