import {formationSlots,assignPlayersToFormation} from './lineup-state.js';
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const esc=v=>String(v??'').replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]));
const lastName=name=>String(name||'?').trim().split(/\s+/).at(-1)||'?';
function tokens(team,plan){const formation=plan?.preferredFormation||'4-3-3',slots=formationSlots(formation),assigned=assignPlayersToFormation(plan?.starters||[],formation,(plan?.starters||[]).map(p=>String(p.id))),byId=new Map((plan?.starters||[]).map(p=>[String(p.id),p]));return slots.map(([key,slot,sx,sy],i)=>{const a=assigned.find(x=>x.key===key),p=byId.get(String(a?.playerId||''))||plan?.starters?.[i]||{},ax=team===0?100-sy:sy;return{team,id:String(p.id||`${team}-${key}`),name:p.name||slot,overall:Number(p.originalOverall||p.overall||plan?.lineupOverall||70),slot,ax,ay:sx,x:ax,y:sx,tx:ax,ty:sx,phase:i*.7}})}
function eventMeta(type){return({GOAL:{icon:'⚽',label:'GOL',cls:'goal'},SHOT_ON:{icon:'◎',label:'FINALIZAÇÃO',cls:'shot'},SHOT_OFF:{icon:'↗',label:'FINALIZAÇÃO',cls:'shot'},OFFSIDE:{icon:'🚩',label:'IMPEDIMENTO',cls:'offside'},CARD:{icon:'■',label:'CARTÃO AMARELO',cls:'card'},INJURY:{icon:'✚',label:'LESÃO',cls:'injury'},SUB:{icon:'⇄',label:'SUBSTITUIÇÃO',cls:'sub'},TACTIC:{icon:'↗',label:'AJUSTE TÁTICO',cls:'tactic'},CORNER:{icon:'◩',label:'ESCANTEIO',cls:'corner'},FOUL:{icon:'!',label:'FALTA',cls:'foul'},HALF:{icon:'Ⅱ',label:'INTERVALO',cls:'period'},FULL:{icon:'■',label:'FIM DE JOGO',cls:'period'}})[type]||{icon:'•',label:'LANCE',cls:'normal'}}
function fmtCrowd(n){return Number(n||0).toLocaleString('pt-BR')}
export function showMatchViewer({app,club,fixture,result,coachName,onFinish}){
 const userTeam=fixture.home?0:1,homeName=fixture.home?club.name:fixture.opponentName,awayName=fixture.home?fixture.opponentName:club.name,userPlan=result.plan||{},oppPlan=result.opponentPlan||{},homePlan=userTeam===0?userPlan:oppPlan,awayPlan=userTeam===1?userPlan:oppPlan,details=result.detailedStats||{user:{},opponent:{}},homeStats=userTeam===0?details.user||{}:details.opponent||{},awayStats=userTeam===1?details.user||{}:details.opponent||{},timeline=[...(result.timeline||[])].sort((a,b)=>Number(a.minute||0)-Number(b.minute||0)),passes=[...(result.possessionScript||[])].sort((a,b)=>Number(a.minute||0)-Number(b.minute||0)),attendance=result.attendance||{count:0,capacity:0,occupancy:0,venue:''};
 app.innerHTML=`<section class="gcp-match-live"><header class="gcp-match-head"><button id="gcpMatchSkip" class="gcp-match-tool">PULAR</button><div class="gcp-match-score"><span>${esc(fixture.competition)} • ${esc(fixture.round||'PARTIDA')}</span><div><b>${esc(homeName)}</b><strong><i id="gcpHomeScore">0</i><em>–</em><i id="gcpAwayScore">0</i></strong><b>${esc(awayName)}</b></div><small><b id="gcpMinute">0</b>' • ${fmtCrowd(attendance.count)} torcedores</small></div><div class="gcp-match-speed"><button id="gcpPause" aria-label="Pausar">Ⅱ</button>${[1,2,4,8].map(x=>`<button data-match-speed="${x}" class="${x===1?'active':''}">${x}x</button>`).join('')}</div></header><div class="gcp-match-body"><main class="gcp-match-stage"><div class="gcp-match-pitch"><canvas id="gcpMatchCanvas"></canvas><div class="gcp-match-action"><span id="gcpPhase">BOLA ROLANDO</span><b id="gcpAction">As equipes ocupam os espaços e disputam a posse.</b></div><div id="v5Flash" class="gcp-match-flash"></div><div id="v5End" class="gcp-match-end" hidden></div></div><div class="gcp-match-stats"><div><span>Posse</span><b id="gcpPoss">50–50</b></div><div><span>Finalizações</span><b id="gcpShots">0–0</b></div><div><span>xG</span><b id="gcpXg">0.00–0.00</b></div><div><span>Impedimentos</span><b id="gcpOffsides">0–0</b></div><div><span>Público</span><b>${fmtCrowd(attendance.count)}</b></div></div></main><aside class="gcp-match-feed-wrap"><div class="gcp-match-context"><div><span>PLANO</span><b>${esc(userPlan.preferredFormation||'Variável')} • ${esc(userPlan.style||userPlan.coachProfile?.style||'Equilibrado')}</b></div><div><span>ESTÁDIO</span><b>${esc(attendance.venue||'Estádio')} • ${attendance.occupancy||0}%</b></div></div><h3>Acontecimentos</h3><div id="gcpMatchFeed" class="gcp-match-feed" aria-live="polite"><div class="gcp-event normal"><strong>0'</strong><i>●</i><span><b>INÍCIO</b><small>Bola rolando.</small></span></div></div></aside></div></section>`;
 const canvas=document.querySelector('#gcpMatchCanvas'),ctx=canvas?.getContext('2d'),all=[...tokens(0,homePlan),...tokens(1,awayPlan)],ball={x:50,y:50,owner:null,flight:null},score=[0,0];let minute=0,speed=1,paused=false,ended=false,eventIndex=0,passIndex=0,last=performance.now(),acc=0,raf=0;const msPerMinute=1120;
 const teamIndex=side=>side==='USER'?userTeam:1-userTeam;
 const findPlayer=(team,id,name)=>all.find(p=>p.team===team&&String(p.id)===String(id))||all.find(p=>p.team===team&&p.name===name)||null;
 function lookup(plan,id,name){return[...(plan?.starters||[]),...(plan?.bench||[])].find(p=>String(p.id)===String(id))||[...(plan?.starters||[]),...(plan?.bench||[])].find(p=>p.name===name)||null}
 function resize(){if(!canvas||!ctx)return;const dpr=Math.min(2,window.devicePixelRatio||1),w=Math.max(1,canvas.clientWidth),h=Math.max(1,canvas.clientHeight);canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}resize();window.addEventListener('resize',resize,{passive:true});
 function owner(p){if(!p)return;ball.owner=p;ball.flight=null;ball.x=p.x;ball.y=p.y}
 owner(all.find(p=>p.team===0&&p.slot!=='GK')||all[0]);
 function flight(from,to,outcome='PASS',next=null){if(!from||!to)return;ball.owner=null;ball.x=from.x;ball.y=from.y;ball.flight={toX:to.x,toY:to.y,outcome,next,progress:0}}
 function shot(actor,goal,kind){if(!actor)return;owner(actor);ball.owner=null;ball.flight={toX:goal.x,toY:goal.y,outcome:kind,next:null,progress:0}}
 function phase(text,label='POSSE'){const a=document.querySelector('#gcpAction'),p=document.querySelector('#gcpPhase');if(a)a.textContent=text;if(p)p.textContent=label}
 function flash(text,kind=''){const el=document.querySelector('#v5Flash');if(!el)return;el.className=`gcp-match-flash ${kind} show`;el.textContent=text;clearTimeout(el._timer);el._timer=setTimeout(()=>el.classList.remove('show'),1000)}
 function feed(e,text=e.text||''){const root=document.querySelector('#gcpMatchFeed');if(!root)return,m=eventMeta(e.type),row=document.createElement('div');row.className=`gcp-event ${m.cls}`;row.innerHTML=`<strong>${Math.floor(Number(e.minute)||0)}'</strong><i>${m.icon}</i><span><b>${m.label}</b><small>${esc(text||m.label)}</small></span>`;root.prepend(row);while(root.children.length>20)root.lastElementChild.remove()}
 function processPass(e){const team=teamIndex(e.team),from=findPlayer(team,e.passerId,e.passerName),to=findPlayer(team,e.receiverId,e.receiverName),winner=e.winnerId?findPlayer(1-team,e.winnerId,e.winnerName):null;if(e.outcome==='COMPLETE'){phase(`${lastName(e.passerName)} toca para ${lastName(e.receiverName)}`,'CIRCULAÇÃO');flight(from,to,'PASS',to)}else if(e.outcome==='RECOVERED'){phase(`${lastName(e.winnerName)} antecipa e recupera`,'RECUPERAÇÃO');flight(from,to,'RECOVERY',winner);feed({type:'FOUL',minute:e.minute},`${e.winnerName} lê a jogada e recupera a posse.`)}else{phase('Passe pressionado. A bola fica viva.','DISPUTA');flight(from,to,'LOOSE',null)}}
 function processEvent(e){if(e.team){const team=teamIndex(e.team),actor=findPlayer(team,e.shooterId||e.playerId,e.shooterName||e.playerName);if(actor)owner(actor);if(e.type==='GOAL'){const goal={x:team===0?98:2,y:clamp(actor?.y||50,43,57)};shot(actor,goal,'GOAL');score[team]++;document.querySelector('#gcpHomeScore').textContent=score[0];document.querySelector('#gcpAwayScore').textContent=score[1];flash(`GOL! ${e.shooterName||e.playerName||''}`,'goal');phase(e.text,`⚽ GOL • ${e.shooterName||e.playerName||''}`);feed(e)}else if(e.type==='SHOT_ON'){shot(actor,{x:team===0?98:2,y:50},'SHOT');flash('DEFESA!','shot');phase(e.text,'FINALIZAÇÃO');feed(e)}else if(e.type==='SHOT_OFF'){shot(actor,{x:team===0?101:-1,y:(actor?.y||50)+(actor?.y>50?10:-10)},'SHOT');phase(e.text,'FINALIZAÇÃO');feed(e)}else if(e.type==='OFFSIDE'){flash('IMPEDIMENTO','offside');phase(e.text,'🚩 IMPEDIMENTO');feed(e)}else if(e.type==='CARD'){flash('CARTÃO AMARELO','card');phase(e.text,'CARTÃO');feed(e)}else if(e.type==='INJURY'){flash(`LESÃO • ${e.playerName||''}`,'injury');phase(`${e.text} ${e.injuryName?`Diagnóstico: ${e.injuryName}.`:''}`,'✚ ATENDIMENTO');feed(e,`${e.text}${e.days?` • previsão: ${e.days} dias`:''}`)}else if(e.type==='SUB'){const token=findPlayer(team,e.outId,e.outName),plan=team===userTeam?userPlan:oppPlan,incoming=lookup(plan,e.inId,e.inName);if(token){token.id=String(e.inId||token.id);token.name=e.inName||incoming?.name||token.name;token.overall=Number(incoming?.overall||token.overall)}phase(e.text,'SUBSTITUIÇÃO');feed(e)}else if(e.type==='TACTIC'){phase(e.text,'AJUSTE TÁTICO');feed(e)}else if(e.type==='CORNER'){phase(e.text,'ESCANTEIO');feed(e)}else if(e.type==='FOUL'){phase(e.text,'FALTA');feed(e)}}else if(e.type==='HALF'){phase('Intervalo. Os técnicos ajustam as equipes.','INTERVALO');feed(e,'Intervalo.')}else if(e.type==='FULL'){feed(e,'Fim de jogo.')}updateStats()}
 function movement(dt,now){const poss=ball.owner?.team??(ball.flight?.next?.team??0);for(const p of all){const dir=p.team===0?1:-1,has=p.team===poss,plan=p.team===userTeam?userPlan:oppPlan,profile=plan?.coachProfile||{},press=Number(profile.pressing||55),line=Number(profile.defensiveLine||52),width=Number(profile.width||55),drift=Math.sin(now/580+p.phase)*1.6,driftY=Math.cos(now/710+p.phase*.8)*1.3;let tx=p.ax,ty=50+(p.ay-50)*(1+(width-50)/250);if(has){const advance=/ST|LW|RW|AM/.test(p.slot)?11:/CM|LM|RM|WB/.test(p.slot)?6:3;tx+=dir*advance;ty+=(ball.y-ty)*(/ST|LW|RW|AM|CM/.test(p.slot)?.18:.08);if(ball.owner&&String(ball.owner.id)!==String(p.id)){const d=Math.hypot(p.x-ball.owner.x,p.y-ball.owner.y);if(d<28){tx+=dir*4;ty+=(p.y<ball.owner.y?-1:1)*4}}}else{tx-=dir*4;if(/CB|LB|RB|WB/.test(p.slot))tx+=dir*(line-50)/8;const d=Math.hypot(p.x-ball.x,p.y-ball.y);if(d<30&&press>50){const close=.18+(press-50)/180;tx+=(ball.x-p.x)*close;ty+=(ball.y-p.y)*close}}if(p.slot==='GK'){tx=p.ax;ty=clamp(50+(ball.y-50)*.14,35,65)}tx=clamp(tx+drift,3,97);ty=clamp(ty+driftY,3,97);const dx=tx-p.x,dy=ty-p.y,d=Math.hypot(dx,dy),step=(11+p.overall/17)*dt;if(d>.02){const k=Math.min(1,step/d);p.x+=dx*k;p.y+=dy*k}}
 if(ball.owner){ball.x=ball.owner.x;ball.y=ball.owner.y}else if(ball.flight){const f=ball.flight;f.progress=clamp(f.progress+dt*3.25,0,1),ease=1-Math.pow(1-f.progress,2);ball.x+=(f.toX-ball.x)*Math.min(1,dt*(7+ease*5));ball.y+=(f.toY-ball.y)*Math.min(1,dt*(7+ease*5));if(f.progress>=1){const next=f.next;ball.flight=null;if(next)owner(next)}}}
 function draw(){if(!ctx||!canvas)return;const w=canvas.clientWidth,h=canvas.clientHeight,px=x=>x/100*w,py=y=>y/100*h;ctx.clearRect(0,0,w,h);ctx.fillStyle='#31995a';ctx.fillRect(0,0,w,h);ctx.strokeStyle='rgba(255,255,255,.86)';ctx.lineWidth=2;ctx.strokeRect(10,10,w-20,h-20);ctx.beginPath();ctx.moveTo(w/2,10);ctx.lineTo(w/2,h-10);ctx.stroke();ctx.beginPath();ctx.arc(w/2,h/2,Math.min(w,h)*.13,0,Math.PI*2);ctx.stroke();ctx.strokeRect(10,h*.29,w*.13,h*.42);ctx.strokeRect(w*.87,h*.29,w*.13-10,h*.42);for(const p of all){ctx.beginPath();ctx.fillStyle=p.team===0?'#f6fbff':'#173c68';ctx.strokeStyle=p.team===0?'#0b5f93':'#fff';ctx.lineWidth=2;ctx.arc(px(p.x),py(p.y),8.5,0,Math.PI*2);ctx.fill();ctx.stroke();ctx.fillStyle=p.team===0?'#12364d':'#fff';ctx.font='700 8px Inter,sans-serif';ctx.textAlign='center';ctx.fillText(String(p.overall),px(p.x),py(p.y)+3);ctx.fillStyle='#fff';ctx.strokeStyle='rgba(0,0,0,.45)';ctx.lineWidth=3;ctx.font='700 9px Inter,sans-serif';ctx.strokeText(lastName(p.name),px(p.x),py(p.y)+20);ctx.fillText(lastName(p.name),px(p.x),py(p.y)+20)}ctx.beginPath();ctx.fillStyle='#fff';ctx.strokeStyle='#1e293b';ctx.lineWidth=1.5;ctx.arc(px(ball.x),py(ball.y),4.6,0,Math.PI*2);ctx.fill();ctx.stroke()}
 function updateStats(){const processed=timeline.filter(e=>Number(e.minute||0)<=minute),side=t=>teamIndex(t),hs=processed.filter(e=>e.team&&side(e.team)===0&&['GOAL','SHOT_ON','SHOT_OFF'].includes(e.type)).length,as=processed.filter(e=>e.team&&side(e.team)===1&&['GOAL','SHOT_ON','SHOT_OFF'].includes(e.type)).length,ho=processed.filter(e=>e.type==='OFFSIDE'&&side(e.team)===0).length,ao=processed.filter(e=>e.type==='OFFSIDE'&&side(e.team)===1).length,hx=processed.filter(e=>e.team&&side(e.team)===0).reduce((s,e)=>s+Number(e.xg||0),0),ax=processed.filter(e=>e.team&&side(e.team)===1).reduce((s,e)=>s+Number(e.xg||0),0),progress=clamp(minute/90,0,1),final=Number(homeStats.possession||50),poss=Math.round(50+(final-50)*(.25+progress*.75));document.querySelector('#gcpPoss').textContent=`${poss}–${100-poss}`;document.querySelector('#gcpShots').textContent=`${hs}–${as}`;document.querySelector('#gcpXg').textContent=`${hx.toFixed(2)}–${ax.toFixed(2)}`;document.querySelector('#gcpOffsides').textContent=`${ho}–${ao}`}
 function finish(){if(ended)return;ended=true;minute=90;while(passIndex<passes.length)processPass(passes[passIndex++]);while(eventIndex<timeline.length)processEvent(timeline[eventIndex++]);document.querySelector('#gcpMinute').textContent='90';updateStats();const end=document.querySelector('#v5End'),best=result.bestPlayer;end.hidden=false;end.innerHTML=`<span>FIM DE JOGO</span><h2>${esc(homeName)} <b>${score[0]}–${score[1]}</b> ${esc(awayName)}</h2><div class="gcp-match-end-facts"><p><b>${fmtCrowd(attendance.count)}</b> torcedores • ${attendance.occupancy||0}% da capacidade</p>${best?`<p>Melhor em campo: <b>${esc(best.name)}</b> • ${Number(best.rating||0).toFixed(1)}</p>`:''}</div><button id="gcpFinishMatch">FINALIZAR PARTIDA</button>`;document.querySelector('#gcpFinishMatch').onclick=()=>{cancelAnimationFrame(raf);window.removeEventListener('resize',resize);onFinish?.()}}
 function tick(now){if(ended)return;const dt=Math.min(.05,(now-last)/1000);last=now;if(!paused){acc+=dt*1000*speed;while(acc>=msPerMinute&&minute<90){acc-=msPerMinute;minute++;document.querySelector('#gcpMinute').textContent=String(minute);while(passIndex<passes.length&&Number(passes[passIndex].minute||0)<=minute)processPass(passes[passIndex++]);while(eventIndex<timeline.length&&Number(timeline[eventIndex].minute||0)<=minute)processEvent(timeline[eventIndex++]);if(minute>=90){finish();return}}movement(dt*speed,now)}draw();raf=requestAnimationFrame(tick)}
 document.querySelectorAll('[data-match-speed]').forEach(b=>b.onclick=()=>{speed=Number(b.dataset.matchSpeed)||1;document.querySelectorAll('[data-match-speed]').forEach(x=>x.classList.toggle('active',x===b))});document.querySelector('#gcpPause').onclick=e=>{paused=!paused;e.currentTarget.textContent=paused?'▶':'Ⅱ'};document.querySelector('#gcpMatchSkip').onclick=finish;updateStats();raf=requestAnimationFrame(tick)
}
